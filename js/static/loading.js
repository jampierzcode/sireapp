$(document).ready(()=>{
    // $(".show-loading").removeClass("show-loading");
    // $(".loading-page").addClass("hidden_load");

    AOS.init({
        easing: 'ease-out-back',
        duration: 1000
    });
    // ScrollOut();
})